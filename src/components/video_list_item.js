//initial default components
import React from 'react';

//our components
const VideoListItem = () => {
  return <div>
      Finally, these are the list item.
  </div>
};

//exporting components
export default VideoListItem;